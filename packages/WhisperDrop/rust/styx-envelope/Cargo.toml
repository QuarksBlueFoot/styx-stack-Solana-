[package]
name = "styx-envelope"
version = "0.2.4"
edition = "2021"
license = "MIT"
description = "Styx Envelope v1 canonical encoder/decoder (clean-room)"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
